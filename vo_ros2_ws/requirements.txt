# Core deep learning
torch>=2.6.0
torchvision>=0.21.0

# SSM libraries (must be installed with --no-build-isolation after torch)
# pip install mamba-ssm causal-conv1d --no-build-isolation
mamba-ssm>=2.3.0
causal-conv1d>=1.6.0

# Vision / geometry
opencv-python>=4.9.0
numpy>=2.0.0
scipy>=1.13.0
kornia>=0.8.0

# Trajectory evaluation
evo>=1.36.0
# Upper bound avoids mpl_toolkits namespace conflict with system python3-matplotlib on Ubuntu
matplotlib>=3.7.0,<3.9

# GPU profiling
nvidia-ml-py3>=7.352.0

# ROS2 bridge (installed via apt, not pip)
# sudo apt install ros-humble-cv-bridge ros-humble-vision-opencv
